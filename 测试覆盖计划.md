# 测试覆盖计划 · Test Plan

> 版本：v1.0（2026-09-02）
> 定位：CI（`.github/workflows/ci.yml` → pytest）所守护的业务规则清单与补全计划。
> **本文件是活文档**：每次新增/修改功能时，其测试要求必须同步登记到本文档（见第四节维护约定）。

---

## 一、目标与原则

1. **CI 跑的是 pytest 用例，用例背后是业务规则**——本文档回答"哪些规则必须有测试护栏"。
2. 护栏优先级 = 出错代价：**会计恒等式与价税口径 > 凭证状态与查重 > AI 护栏 > 展示/导出**。
3. 每条 AGENTS.md「业务硬约束（红线）」必须有至少一条 API 级或函数级用例；纯前端交互（缩放、拖拽）不强制。
4. 测试写法遵循现有模式：conftest.py 公共 fixture（book/mama_user/auditor_user/post_flow/contacts_pair/mock_month），函数级直测 + TestClient API 级场景测试。

## 二、现状盘点（2026-09-02，158 用例）

### ✅ 已覆盖（不需要动）

| 规则域 | 文件 | 用例数 | 覆盖点 |
|---|---|---|---|
| 凭证录入校验 | test_voucher_rules.py | 12 | 借贷平衡/科目合法/停用科目/非叶子/零金额/摘要/手工凭证附件必附/期间早于启用拒绝/金额精度往返 |
| 凭证状态流转 | test_voucher_workflow.py | 6 | 草稿→审核→过账→反过账、岗位分离 |
| AI 结构化校验 | test_ai_suggest.py | 6 | 未知科目/不平衡/非JSON/金额不符警告/红字提示 |
| AI 智能体护栏 | test_ai_agent.py | 6 | 工具白名单/ask_user 终止/工具真实执行/跨账套隔离/购方 guard |
| 财务报表 | test_financial_statements.py | 5 | BS 精确数与恒等式/叶子科目全覆盖一次/IS 结转前后/净利润勾稽 |
| 附件 | test_attachments.py | 7 | 上传/真删/锁定 |
| 其余 | 账套/科目/发票导入/税务/结账/备份/导出/迁移/网关等 | 110+ | 略（见各文件） |

### ❌ 缺口（本计划要补的，2026-09-02 上线批次）

| # | 规则（出处） | 现状 |
|---|---|---|
| G1 | cf_item 现金流量行级标注 + `_apply_cf_items` 兜底 | test_cashflow.py 仅 1 条旧推断用例 |
| G2 | `_hard_check_duplicate` 重复检测（含"仅已入账算重"） | 零用例 |
| G3 | `_sanitize_contacts` 往来 ID 净化 | 零用例 |
| G4 | `_tax_rule` 按纳税人性质注入价税口径 | 零用例 |
| G5 | 表结法：**未结转时** BS 恒等式 | BS 用例全用结转后 fixture |
| G6 | 映射体检 3 接口（template-check / template-row / template-reset） | 零用例 |
| G7 | period_summary 本期概要 | 零用例 |
| G8 | `_link_invoice` / `_create_invoice_from_fields` 发票回写与补录 | 零用例 |

## 三、补全计划（8 组 / 24 条用例）

### G1 现金流量行级标注（→ 扩充 test_cashflow.py，+5）

| 用例 | 验证点 |
|---|---|
| test_cf_item_valid_kept | 现金行带合法 cf_item（如 `sales_goods`）→ 原样持久化到 voucher_line.cf_item |
| test_cf_item_invalid_on_cash_line_falls_back | 现金行 cf_item 为非法字符串 → 按"对方最大行科目"映射兜底（收现金走 INFLOW_MAP，付现金走 OUTFLOW_MAP） |
| test_cf_item_missing_on_cash_line_falls_back | 现金行缺 cf_item → 同上兜底，绝不为 None |
| test_cf_item_cleared_on_non_cash_line | 非现金行即便提交了 cf_item → 落库前被清除 |
| test_one_voucher_multi_cf_items | 一张凭证两行现金（收款+付款）→ 各自标注不同流量项，现金流量表正确拆分 |

### G2 重复检测（→ 新建 test_duplicate_check.py，+4）

| 用例 | 验证点 |
|---|---|
| test_hard_check_duplicate_hit | 存在相同金额±30天窗口凭证 → `_hard_check_duplicate` 返回含警告标记的文本（含既有凭证号） |
| test_hard_check_duplicate_no_hit | 金额或日期窗口不匹配 → 返回 None（放行，无误伤） |
| test_dup_invoice_only_counts_posted | **发票号查重只算已入账**：台账已导入未生成凭证的发票不算重复（voucher_id NOT NULL 判定） |
| test_dup_already_confirmed_not_looping | 历史消息已含警告标记 → `_dup_already_confirmed` 为 True，不再二次拦截（防死循环） |

### G3 往来净化（→ 扩充 test_ai_agent.py 或随 G2 新文件，+2）

| 用例 | 验证点 |
|---|---|
| test_sanitize_contacts_clears_fabricated_id | 模型编造不存在的 contact_id=99999 → 落库前置空，返回清理计数 |
| test_sanitize_contacts_keeps_valid_cross_book_rejected | 有效 ID 但属于**另一账套** → 同样置空（book_id 校验）；本账套有效 ID → 保留 |

### G4 价税口径注入（→ 新建 test_tax_rule.py，+3）

| 用例 | 验证点 |
|---|---|
| test_tax_rule_small_scale | taxpayer_type=small_scale → 提示词含"严禁拆税"且不出现"进项税额" |
| test_tax_rule_general | taxpayer_type=general → 提示词含双向价税分离与"销项/进项" |
| test_tax_rule_default_small | book=None → 默认按小规模（安全回退） |

### G5 表结法期中恒等式（→ 扩充 test_financial_statements.py，+2）

| 用例 | 验证点 |
|---|---|
| test_balance_sheet_identity_before_carryover | 用 `mock_month`（**未结转**）→ total_assets == total_liab_equity，恒等式成立 |
| test_undistributed_profit_absorbs_pnl_before_carryover | 未结转时"未分配利润"行 = 3103+3104+全部损益净额（验证公式吸收损益而非取 3103 裸值） |

### G6 映射体检（→ 新建 test_template_check.py，+4）

| 用例 | 验证点 |
|---|---|
| test_template_check_defaults_healthy | 默认模板 → 未映射科目清单中"有余额的"为空、恒等式 True |
| test_unmapped_account_with_balance_flagged | 新增一个有余额的明细科目且不在任何公式 → 出现在体检清单且 has_balance=True |
| test_template_row_edit_and_validate | PUT template-row 修改某行取数科目 → 体检实时反映；非法科目/编辑 calc 行 → 拒绝 |
| test_template_reset_reverts | 编辑后 POST template-reset → 恢复默认模板（幂等，可重复调用） |

### G7 本期概要（→ 新建 test_period_summary.py，+2）

| 用例 | 验证点 |
|---|---|
| test_period_summary_counts_and_kpi | 有数据期间 → 凭证四状态计数正确，收入/净利润/货币资金取数与报表一致 |
| test_period_summary_empty_period_zero | 空期间 → 所有金额字段为 "0.00"（字符串，前端契约），状态计数全 0 |

### G8 发票关联与补录（→ 扩充 test_ai_suggest.py，+4）

| 用例 | 验证点 |
|---|---|
| test_link_invoice_existing_no | 台账已有该发票号 → 落账后 invoice.voucher_id 回写 |
| test_link_invoice_creates_from_pdf_fields | 台账无此号 + source_kind=pdf → 自动补录（字段、方向判定：购方税号=账套→sales，否则 purchase） |
| test_link_invoice_text_no_create | source_kind=text → 不补录（字段不足） |
| test_link_invoice_red_invoice_negative | 红字发票 → 补录金额取负数、status=red |

**验收**：全部补完后 pytest 总数 ≈ 182，CI 三绿（本地容器内跑 + push 后 GitHub Actions 绿），并更新本文档第二节矩阵。

## 四、维护约定（活文档规则）

本文件与功能开发**同步演进**，具体触发点：

1. **新功能上线** → 开发时同步：在第二节矩阵登记新规则域及测试文件；若属红线（AGENTS.md 二类），未配测试不得声称完成（AGENTS.md 4.3 已有此要求，本文档是清单载体）。
2. **规则变更/删除** → 对应用例改写或删除，矩阵同步更新，commit message 注明。
3. **每轮提交** → CI 必须绿；补测试的 commit 单独成条（`test: ...` 前缀），不与功能改动混提。
4. **版本管理** → 每次更新本文件递增版本号+日期（当前 v1.0），重大覆盖范围变化在文末加一行变更记录。
5. **与 README 分工**：README 记"有什么功能"；本文档记"哪些规则被测试锁住"；开发进度与交接.md 记开发细节。

## 变更记录

- v1.0（2026-09-02）：初始盘点 158 用例，识别 G1–G8 缺口，制定 24 条补全计划。
