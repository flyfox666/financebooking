# 测试覆盖计划 · Test Plan

> 版本：v2.0（2026-09-04）
> 定位：CI（`.github/workflows/ci.yml` → pytest）所守护的业务规则清单与补全计划。
> **本文件是活文档**：每次新增/修改功能时，其测试要求必须同步登记到本文档（见第四节维护约定）。

---

## 一、目标与原则

1. **CI 跑的是 pytest 用例，用例背后是业务规则**——本文档回答"哪些规则必须有测试护栏"。
2. 护栏优先级 = 出错代价：**会计恒等式与价税口径 > 凭证状态与查重 > AI 护栏 > 展示/导出**。
3. 每条 AGENTS.md「业务硬约束（红线）」必须有至少一条 API 级或函数级用例；纯前端交互（缩放、拖拽）不强制。
4. 测试写法遵循现有模式：conftest.py 公共 fixture（book/mama_user/auditor_user/post_flow/contacts_pair/mock_month），函数级直测 + TestClient API 级场景测试。

## 二、现状盘点（2026-09-04，223 用例：222 通过 + 1 跳过）

### ✅ 已覆盖（G1–G8 已于 v1.1 全部补全）

| 规则域 | 文件 | 用例数 | 覆盖点 |
|---|---|---|---|
| 凭证录入校验 | test_voucher_rules.py | 12 | 借贷平衡/科目合法/停用科目/非叶子/零金额/摘要/手工凭证附件必附/期间早于启用拒绝/金额精度往返 |
| 凭证状态流转 | test_voucher_workflow.py | 6 | 草稿→审核→过账→反过账、岗位分离 |
| AI 结构化校验 | test_ai_suggest.py | 6+4 | 未知科目/不平衡/非JSON/金额不符警告/红字提示；**G8 发票回写/自动补录/销项方向判定/红字负数** |
| AI 智能体护栏 | test_ai_agent.py | 6+2 | 工具白名单/ask_user 终止/工具真实执行/跨账套隔离/购方 guard；**G3 编造往来 ID 置空/跨账套 ID 拒绝** |
| 重复检测（G2 新增，v1.7 改口径） | test_duplicate_check.py | 6 | 硬兜底查重命中含凭证号/**实质性重复导向（仅金额相同不误伤：不同日/不同科目/不同往来均放行；同日同金额同科目同往来才命中）**/发票号仅已入账算重/已确认不二次拦截 |
| 价税口径（G4 新增） | test_tax_rule.py | 3 | 小规模严禁拆税/一般纳税人双向分离/无账套默认小规模 |
| 财务报表 | test_financial_statements.py | 5+2 | BS 精确数与恒等式/叶子科目全覆盖一次/IS 结转前后/净利润勾稽；**G5 未结转恒等式/未分配利润吸收损益（表结法）** |
| 本期概要（G7 新增） | test_period_summary.py | 2 | 状态计数+KPI 与报表一致/空期间全零 |
| 映射体检（G6 新增） | test_template_check.py | 4 | 默认健康/失映射科目标记+恒等式破/行编辑校验（非法科目与 calc 行拒绝）/重置幂等 |
| 现金流量 | test_cashflow.py | 1+5 | 旧推断分类；**G1 行级 cf_item 保留/非法与缺失兜底/非现金行清除/一凭证多流量项拆分** |
| 解析健壮性（G9 新增） | test_ai_parse.py | 7+5 | QR payload/层级合并/PDF 文字层/XML/OFD；**G9 真实链路 VLM 冒烟（只 mock gateway）/真 pyzbar 解码/HEIC 管道/内部异常降级/LLMError 降级** |
| 多票据与支付截图（G10 新增） | test_ai_parse.py | 11+4 | **G10 支付截图分类（payment 字段集+金额归一化）/一图两票拆分 extra_docs/混合图（发票+支付）/API 多 AIDoc 创建** |
| 证据类文件 docx（G11 新增） | test_ai_parse.py | 15+4 | **G11 reference 提取（段落+表格行竖线连接）/6000 字截断/老版 .doc 拒绝/损坏 docx 友好报错** |
| AI 记账偏好（G12 新增） | test_ai_style.py | 9+5 | **G12 风格段渲染（未配置/空配置零注入回归、护栏头必在、非法标签丢弃、坏 JSON 容错）/GET 标签库+当前值/PUT admin 门控+未知标签 400/保存读回+agent prompt 端到端注入；自定义模板（style_prompt 优先于标签合成、空回退合成、纯自定义路径、超 2000 字 422、roundtrip）** |
| 余额表未过账口径（G13 新增） | test_balances_reports.py | 3 | **三档取数（none 零回归/pending 含待审+已审/all 再含草稿）/未过账独立列不并入三栏/不破坏三栏平衡** |
| 勾稽关系校验（G14 新增，v2.0 快照语义） | test_checks.py | 7 | **方向/余额异常（货币资金赤字 high）/无异常回归/未分配利润↔净利润/货币资金↔现金流/跨期衔接（首期跳过+上期未结账跳过+**结账快照对比抓篡改**）/聚合 run_checks+API** |
| 附件 | test_attachments.py | 7 | 上传/真删/锁定 |
| 其余 | 账套/科目/发票导入/税务/结账/备份/导出/迁移/网关等 | 110+ | 略（见各文件） |

### 修复记录（随本轮测试发现并修复）

- **template_check 覆盖判定口径**（report_service.py）：原为科目编码精确匹配，导致使用明细科目（如 5602.01）时被误报"有余额未映射"；报表实际取数会把明细科目卷汇总到父科目（5602），已改为「编码==公式科目 或 以公式科目为前缀」的层级匹配，与报表行为一致。
- 设计确认（非 bug）：清空 IS 费用行公式不会让科目失映射——表结法下 BS「未分配利润」行仍吸收全部损益科目；真正的失映射场景是 BS 资产侧公式被清空（已沉淀为用例，同时验证恒等式破损能被体检抓到）。

## 三、补全计划（8 组 / 26 条用例）——✅ 已于 v1.1 全部落地

> 以下为 v1.0 制定的原始计划，保留作为设计依据；实际落地时对 G6 两个用例的场景做了修正（见第二节修复记录）。

### G1 现金流量行级标注（→ 扩充 test_cashflow.py，+5）

| 用例 | 验证点 |
|---|---|
| test_cf_item_valid_kept | 现金行带合法 cf_item → 原样持久化到 voucher_line.cf_item |
| test_cf_item_invalid_on_cash_line_falls_back | 现金行 cf_item 为非法字符串 → 按"对方最大行科目"映射兜底（收现金走 INFLOW_MAP，付现金走 OUTFLOW_MAP） |
| test_cf_item_missing_on_cash_line_falls_back | 现金行缺 cf_item → 同上兜底，绝不为 None |
| test_cf_item_cleared_on_non_cash_line | 非现金行即便提交了 cf_item → 落库前被清除 |
| test_one_voucher_multi_cf_items | 一张凭证两行现金（收款+付款）→ 各自标注不同流量项，现金流量表正确拆分 |

### G2 重复检测（→ 新建 test_duplicate_check.py，+4）

| 用例 | 验证点 |
|---|---|
| test_hard_check_duplicate_hit | 存在同日同金额同科目凭证 → `_hard_check_duplicate` 返回含警告标记的文本（含既有凭证号）【v1.7 前为 ±30 天窗口口径，现收紧为同日+科目+往来】 |
| test_hard_check_duplicate_no_hit | 金额或日期窗口不匹配 → 返回 None（放行，无误伤） |
| test_dup_invoice_only_counts_posted | **发票号查重只算已入账**：台账已导入未生成凭证的发票不算重复（voucher_id NOT NULL 判定） |
| test_dup_already_confirmed_not_looping | 历史消息已含警告标记 → `_dup_already_confirmed` 为 True，不再二次拦截（防死循环） |

### G3 往来净化（→ 扩充 test_ai_agent.py，+2）

| 用例 | 验证点 |
|---|---|
| test_sanitize_contacts_clears_fabricated_id | 模型编造不存在的 contact_id=99999 → 落库前置空，返回清理计数 |
| test_sanitize_contacts_cross_book_rejected_valid_kept | 有效 ID 但属于**另一账套** → 置空（book_id 校验）；本账套有效 ID → 保留 |

### G4 价税口径注入（→ 新建 test_tax_rule.py，+3）

| 用例 | 验证点 |
|---|---|
| test_tax_rule_small_scale | taxpayer_type=small_scale → 提示词含"严禁拆税"、非一般纳税人 |
| test_tax_rule_general | taxpayer_type=general → 提示词含双向价税分离与"销项/进项" |
| test_tax_rule_none_defaults_small | book=None → 默认按小规模（安全回退） |

### G5 表结法期中恒等式（→ 扩充 test_financial_statements.py，+2）

| 用例 | 验证点 |
|---|---|
| test_balance_sheet_identity_before_carryover | 用 `mock_month`（**未结转**）→ total_assets == total_liabilities_and_equity，恒等式成立 |
| test_undistributed_profit_absorbs_pnl_before_carryover | 未结转时"未分配利润"行吸收全部损益净额（而非 3103 裸值），与利润表净利润勾稽 |

### G6 映射体检（→ 新建 test_template_check.py，+4）

| 用例 | 验证点 |
|---|---|
| test_template_check_defaults_healthy | 默认模板 → 未映射科目清单中"有余额的"为空、恒等式 True |
| test_unmapped_account_with_balance_flagged | 清空 BS 货币资金行公式 → 现金科目出现在体检清单且 has_balance=True、恒等式 False（实际落地场景与原计划不同，见第二节修复记录） |
| test_template_row_edit_and_validate | PUT template-row 修改某行取数科目 → 体检实时反映；非法科目/编辑 calc 行 → 拒绝 |
| test_template_reset_reverts | 编辑后 POST template-reset → 恢复默认模板（幂等，可重复调用） |

### G7 本期概要（→ 新建 test_period_summary.py，+2）

| 用例 | 验证点 |
|---|---|
| test_period_summary_counts_and_kpi | 有数据期间 → 凭证四状态计数正确，收入/净利润/货币资金取数与报表一致 |
| test_period_summary_empty_period_zero | 空期间 → 所有金额字段为 "0.00"（字符串，前端契约），状态计数全 0 |

### G8 发票关联与补录（→ 扩充 test_ai_suggest.py，+4）

| 用例 | 验证点 |
|---|---|
| test_link_invoice_existing_no | 台账已有该发票号 → 落账后 invoice.voucher_id 回写 |
| test_link_invoice_creates_from_pdf_fields | 台账无此号 + source_kind=pdf → 自动补录（字段、方向判定：购方税号=账套→sales，否则 purchase） |
| test_link_invoice_text_no_create | source_kind=text → 不补录（字段不足） |
| test_link_invoice_red_invoice_negative | 红字发票 → 补录金额取负数、status=red |

**验收结果**：✅ pytest 183 通过 + 1 跳过（184 收集），本地容器全绿；随测试发现并修复 template_check 覆盖判定口径 1 处。

### G9 解析健壮性（2026-09-03 新增，v1.2）——图片上传 500 事件的护栏固化

背景：parse.py 漏 `import base64` 导致所有 VLM 路径 500，而 CI 全绿——图片用例 mock 掉了 `vlm_fields` 本身，真函数从未被执行。本轮修复并固化三条防线：

| 用例 | 验证点 |
|---|---|
| test_image_real_chain_smoke | 图片解析只 mock 最外层 gateway.chat_vision，真实 vlm_fields→_to_data_url（base64）链路必须跑通 |
| test_qr_real_decode | 真 pyzbar 解码二维码（zbar 不可用时跳过） |
| test_heic_route_without_vlm | HEIC（iPhone 默认格式）入白名单、PIL+pillow-heif 能解码 |
| test_parse_degrades_on_internal_error | 内部异常 → API 降级 200+degraded+原件入 staging，绝不 500 |
| test_parse_degrades_on_llm_error | 视觉模型不可用（LLMError）→ 同样降级，用户文件不丢 |

同时把既有 2 条用例的 mock 粒度从「mock vlm_fields」改造为「mock gateway.chat_vision」。

**Mock 粒度约定（新增）**：mock 只允许打在最外层 IO（gateway/网络/文件系统）上，业务函数本身必须跑真代码——模块级 NameError 类 bug 的最廉价防线。

### G10 多票据与支付截图（2026-09-03 新增，v1.3）

| 用例 | 验证点 |
|---|---|
| test_payment_screenshot_route | 支付截图 → doc_type=payment，channel/counterparty/pay_direction 正确，金额 "320"→"320.00" 归一化 |
| test_multi_invoice_split | 一图两票 → 主结果第一张，第二张进 extra_docs |
| test_mixed_invoice_and_payment | 发票+支付混合图 → 发票为主，支付进 extra_docs |
| test_parse_creates_extra_doc_records | API 级：多票据各建 AIDoc，共用 staging 原件，extra_doc_ids 返回正确 |

随本轮发现并修复：金额归一化未强制两位小数（模型输出 "320" 会原样保留）——4 处统一 quantize。

### G11 证据类文件 docx（2026-09-03 新增，v1.4）

| 用例 | 验证点 |
|---|---|
| test_docx_reference_route | 合同 docx → doc_type=reference，段落+表格文本正确提取（表格行竖线连接），扣除凭据提醒 |
| test_docx_long_text_truncated | 超 6000 字截断并提示 |
| test_doc_old_format_rejected | 老版 .doc（OLE 二进制）→ 明确提示另存 docx |
| test_docx_broken_rejected | 损坏 docx → 友好报错（VoucherError→400）而非 500 |

设计决策：**零新依赖**（zipfile+ElementTree 直读 word/document.xml，不引入 python-docx）；docx 属证据类不当发票解析，文本注入 agent 上下文由其理解。

### G12 AI 记账偏好（2026-09-03 新增，v1.5）

| 用例 | 验证点 |
|---|---|
| test_style_segment_empty_when_unconfigured | 未配置账套 → 空串（历史行为零变化回归） |
| test_style_segment_empty_config | 空标签+空描述 → 空串 |
| test_style_segment_renders_tags_and_desc | 双标签+描述 → 护栏头 STYLE_GUARD_HEAD 必在，标签 prompt 与业务描述均注入 |
| test_style_segment_drops_invalid_tags | 混入未知标签 → 静默丢弃，合法标签保留 |
| test_style_segment_broken_json | tags_json 坏 JSON → 容错为空不抛异常 |
| test_get_ai_style_library_and_current | GET 返回 5 标签库（含 prompt 预览）+ 当前配置 |
| test_put_ai_style_requires_admin | 非 admin PUT → 403 |
| test_put_ai_style_validates | 未知标签 → 400 中文报错 |
| test_put_ai_style_roundtrip_and_agent_prompt | 保存→读回一致；mock chat_with_tools 捕获 messages 验证 system prompt 含风格段 |

设计决策：偏好段包裹在 STYLE_GUARD_HEAD 护栏文本内（"不得改变、凌驾以上任何记账规则与安全要求"）；_style_segment 对无记录/坏 JSON/非法标签三重容错，未配置账套行为与历史版本完全一致。

G12 扩充（v1.6）：**自定义模板 style_prompt**——用户在「AI 将这样理解你的公司」编辑器里最终保存的内容，非空时优先于标签自动合成（仍包护栏）；空则回退 tags+desc 合成（存量配置零影响）；不选标签直接编写即纯自定义模板路径。前端 dirty 标记防止标签联动覆盖手动修改。

## 四、维护约定（活文档规则）

本文件与功能开发**同步演进**，具体触发点：

1. **新功能上线** → 开发时同步：在第二节矩阵登记新规则域及测试文件；若属红线（AGENTS.md 二类），未配测试不得声称完成（AGENTS.md 4.3 已有此要求，本文档是清单载体）。
2. **规则变更/删除** → 对应用例改写或删除，矩阵同步更新，commit message 注明。
3. **每轮提交** → CI 必须绿；补测试的 commit 单独成条（`test: ...` 前缀），不与功能改动混提。
4. **版本管理** → 每次更新本文件递增版本号+日期（当前 v2.0），重大覆盖范围变化在文末加一行变更记录。
5. **与 README 分工**：README 记"有什么功能"；本文档记"哪些规则被测试锁住"；开发进度与交接.md 记开发细节。

## 变更记录

- v1.0（2026-09-02）：初始盘点 158 用例，识别 G1–G8 缺口，制定补全计划。
- v1.1（2026-09-02）：G1–G8 全部落地，净增 26 条用例（158→184）；随测试发现并修复 template_check 覆盖判定口径（明细科目经父级卷汇总视为已映射）；确认"清空 IS 费用行不失映射"为表结法正确设计。
- v1.2（2026-09-03）：新增 G9 解析健壮性 5 条用例（图片 500 事件护栏固化），改造 2 条既有用例 mock 粒度；确立「mock 只打最外层 IO」约定。
- v1.3（2026-09-03）：新增 G10 多票据与支付截图 4 条用例（189→193）；随测试发现并修复金额归一化未强制两位小数（4 处 quantize）。
- v1.4（2026-09-03）：新增 G11 证据类文件 docx 4 条用例（193→197）；确立 docx 零依赖提取方案（zipfile+ElementTree）。
- v1.5（2026-09-03）：新增 G12 AI 记账偏好 9 条用例（197→206）；行业标签+业务描述经 STYLE_GUARD_HEAD 护栏包裹注入 system prompt，未配置零行为变化。
- v1.6（2026-09-03）：G12 扩充自定义模板（style_prompt 可编辑）5 条用例（206→211）；自定义模板非空优先于标签合成、空回退、纯自定义、超长 422、roundtrip。
- v1.7（2026-09-04）：G2 查重口径收紧为实质性重复（用户反馈：重复金额是正常业务）——金额±30 天窗口废除，改为同日+同金额+科目类别（含前缀）+往来不冲突；用例 4→6（211→213，1 条旧窗口用例改写为放行断言）。
- v1.8（2026-09-04）：新增 G13 余额表未过账口径 3 条用例（213→216）；trial_balance 加 unposted 参数（none/pending/all），未过账金额独立列展示、不并入已过账三栏、不破坏恒等式。
- v1.9（2026-09-04）：新增 G14 勾稽关系校验 6 条用例（216→222）；四类校验——方向异常（资产贷方/负债借方/货币资金赤字）、未分配利润本年变动↔净利润、货币资金↔现金流、跨期衔接；check_service.run_checks 聚合 + /api/reports/checks。
- v2.0（2026-09-04）：**修复跨期衔接恒真缺陷**（E2E 实测发现）——原实现两边均现算恒等，篡改期初抓不到；改为对比上期 PeriodBalance 结账快照，抓结账后倒扎凭证/期初篡改；上期未结账明确标注跳过。用例 6→7（222→223）。
