# 有数 LedgerAI · AI 财务记账系统

> 面向小规模纳税人的对话式智能记账系统：把发票、票据、一句话业务描述丢给 AI 助手，
> 生成候选凭证 → 人工确认生成草稿 → 另一人审核 → 过账 → 自动汇总账簿、报表与申报数据。
> **AI 只负责"把业务翻译成凭证"，账目正确性永远不押在模型身上。**

- 文档版本：v2.6（2026-09-07，经营概览与税务核对）· **每次功能更新后须同步本文件**（阶段/功能/亮点章节）
- 详细开发日志与交接细节见 [开发进度与交接.md](开发进度与交接.md)，AI 行为规范见 [AGENTS.md](AGENTS.md)

---

## 一、项目背景

用户自己经营一家一人有限公司（OPC，上海注册的小规模纳税人），账务此前由家里长辈
（非专业会计）用 Excel 手工记，代账公司又是个黑盒。这个项目用"确定性会计内核 +
AI 智能体外壳"的架构给自家公司做一个自用、轻量、可信赖的记账系统：
家人只管把发票丢进对话框，AI 生成凭证草稿，人核对后落账，报表和申报数据全自动出。

## 二、当前阶段（2026-09-07）

按最初方案的 6 个里程碑（M1 地基 → M2 凭证内核 → M3 账簿报表 → M4 容器部署 → M5 报税 → M6 AI 外壳）：

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M1 地基（科目/账套/认证） | ✅ 完成 | 小企业会计准则 66 科目、账套管理、用户角色 |
| M2 凭证内核 | ✅ 完成 | 凭证全生命周期 + 岗位分离 + 红字冲销 + 期末结转 |
| M3 账簿报表 | ✅ 完成 | 余额表/资产负债表/利润表/现金流量表 + Excel 导出 |
| M4 容器部署 | ✅ 完成 | Docker 单容器，数据卷持久化，每日自动备份 |
| M5 报税模块 | 申报辅助持续完善 | 小规模季度增值税汇总、企税预缴核对、个税/印花税计算、带来源的申报日历；不直连税局，未覆盖完整申报 |
| M6 AI 智能体外壳 | ✅ 主体完成 | 对话式 AI 记账 + 发票模块 + 三层护栏（详见下文） |

**当前定位：核心功能闭环已跑通并进入日常使用打磨期**（AI 记账偏好模板化、勾稽关系校验、
余额表未过账口径切换、经营与资金概览、税务核对均已上线）。下一步重心：面向小企业的业务引导、票据池与月结检查，建立真实匿名单据评测集
（见交接文档待办）。

### 2026-09-07 经营概览与税务核对（本轮）

- **本期经营**：本月/本年累计切换，常显收入、成本、毛利、毛利率、期间费用、净利润；展开销售/管理/财务费用、税金及附加、投资收益、营业利润、营业外收支、利润总额和所得税费用。提供收入、净利润较上月的金额变化。
- **资金与往来**：期末账面资金、本月/本年资金变化、经营现金净额，展开现金/银行/其他货币资金及预收预付、其他应收应付。沿用已过账报表口径；金额可下钻到分录并打开原凭证。余额明细包含建账期初说明，尚不支持账龄或逾期判定。
- **独立入口**：左侧新增“经营与资金”，在手机和平板隐藏右栏时仍可查看概览。缺失映射显示待核对，零收入不计算毛利率；提示未过账凭证和计提完整性。
- **税务核对**：2026年日历采用税务总局公布的期限，修正工资薪金所属期及跨年提醒；未知年度标注“待核验”。增值税可切换按月/按季日历视图，此选择仅保存在当前浏览器，不替代税务登记。
- **企业所得税**：用累计利润总额起算，输入截至本期人数/资产季度平均值、行业条件、已预缴金额及预缴调整净额。资料不全时不输出税额；Excel 与页面采用相同输入，并保留待核对状态。当前辅助估算仅核验2026—2027年普通居民公司企业的相关政策，非公司主体或其他年度不输出税额。
- **适用边界**：增值税仍为小规模季度辅助汇总，不能用于按月填报；一般纳税人组合导出已阻止。印花税、财务报表报送等期限须依主管税务机关认定另行核对。此轮未替换小企业会计准则报表，也未增加自动报税。

接口：`GET /api/reports/period-summary` 增加利润构成、资金变化和完整性提示；`GET /api/reports/summary-detail` 按 `book_id/period/key/scope` 分页追溯（`key` 为 `is:net_profit`、`bs:monetary_funds` 等允许项）。`/api/tax/cit` 与 `/api/tax/export` 共用 `employees/assets/industry_eligible/prepaid_prev/adjustment_net/adjustments_confirmed`；`/api/tax/calendar` 与 `/api/tax/reminders` 支持账套校验和 `vat_frequency`。

政策依据：[2026年申报纳税期限](https://fgk.chinatax.gov.cn/zcfgk/c102424/c5245729/content.html)、[企业所得税预缴申报表](https://12366.chinatax.gov.cn/bzds/059/059.html)、[小型微利企业所得税政策](https://fgk.chinatax.gov.cn/zcfgk/c100024/c5236888/content.html)。会计准则与纳税人身份须分开判断；[财会〔2026〕11号](https://ah.mof.gov.cn/tongzhitonggao/202608/t20260810_3995173.htm)的修订不意味着增值税小规模纳税人一律立即换表。本项目本轮继续使用现有小企业会计准则模板，后续按适用准则和生效日期做版本管理。

### 2026-09-07 账务控制与 AI 修复（上一轮）

当前开发入口是 `http://127.0.0.1:18000/app`，使用 `docker compose -f docker-compose.test.yml build backend` 与 `docker compose -f docker-compose.test.yml up -d backend`。这是独立测试环境，旧 8000 端口容器及数据未升级。

- 批量操作逐条核对账套；审核岗禁止编辑凭证；创建人及所有修改人不能审核；真实原件校验贯穿提交、审核、过账；原件变更退回草稿重审。
- 发票按账套、方向、号码关联，购方为本公司是进项；红字金额保留负数；重复红冲受到数据库约束。
- AI 确认具有事务与重试保护；原件写入失败不会遗留凭证；同图多单据共享原件在全部处理前保留。疑似重复/金额差异须核对风险并填写理由，聊天不能授权。
- 多候选卡片独立编辑，切账套丢弃旧上下文和回包；刷新后可从“待处理单据”恢复。支持原件查看，用户/模型文本转义显示。
- AI 按账套纳税人类型生成建议，小规模进项拆税代码拦截；明确银行费用可解释纠正科目；使用校验状态而非虚构置信度；AI 不自动新增往来档案。
- 模型凭据由安装独有密钥加密；管理员可下载包含数据库、原件、待处理文件与解密文件的完整 ZIP，备份失败可见，启动补做备份，成功备份保留30天。
- Windows 启动器通过实例标识识别已有服务，端口被其他程序占用时选用空闲端口。安装包尚未在本目录重新构建，不能把源码修复视为安装包已发布。

复测命令：`docker compose -f docker-compose.test.yml --profile tests run --rm tests`；最终证据、剩余产品工作见交接文档。当前税务输出仍为申报辅助结果，不应将软件测试通过等同于税务口径已完成专业审定。

## 三、总体架构

```
┌────────────────────── Docker 单容器 ledgerai-backend : 8000 ──────────────────────┐
│                                                                                  │
│  前端（原生 HTML/CSS/JS 单页应用，无框架无构建）                                     │
│  static/app.html ── 主界面：凭证/账簿/报表/发票/科目/往来/期末/映射体检 + AI 对话视图  │
│  static/index.html ── 登录/设置                                                    │
│         │ HTTP/JSON                                                               │
│  后端 FastAPI（api/ 13 个路由模块）                                                 │
│  ├── ledger/voucher_service ── 凭证校验落账（借贷平衡/附件/现金流量兜底）             │
│  ├── ledger/report_service ── 报表取数（ReportTemplate 模板驱动 + 表结法）           │
│  ├── ledger/cashflow ── 现金流量（行级标注优先，未标注兜底推断）                      │
│  ├── ledger/check_service ── 勾稽校验（方向异常/利润·现金勾稽/跨期衔接快照对比）        │
│  ├── ledger/tax ── 税务计算                                                        │
│  ├── ledger/ai/agent ── AI 智能体（system prompt + 工具白名单 + 硬护栏）             │
│  └── ledger/ai/suggest/parse/packs ── 解析·候选凭证·台账关联·行业偏好库               │
│         │ SQLAlchemy 2.0                                                          │
│  SQLite /data/ledger.db（单文件，卷挂载宿主 data/，每日 3:00 自动备份）               │
└──────────────────────────────────────────────────────────────────────────────────┘
          │ HTTPS（多 provider gateway，密钥加密存储于 DB）
     云端 LLM（火山/通义/OpenAI 兼容，可切换）
```

**核心架构决策：AI 无任何过账/审核权限**——智能体只能产出"候选凭证"，
工具白名单仅查询类 + ask_user；落账必须由人在凭证卡片上点击确认，经借贷平衡、
科目合法性、重复检测等硬校验后进入凭证流程（草稿→审核→过账）。

## 四、已实现功能

### 核心账务
- 凭证全生命周期：草稿 → 提交审核 → 审核通过/驳回 → 过账 → 反过账，角色权限（制单/审核/管理员岗位分离）
- 小企业会计准则 66 科目、明细科目管理、账套管理、期末结转
- **多账套管理与切换**：顶栏账套下拉切换（期间/对话历史/主数据按账套隔离防串账）；
  新建账套自动预置科目/模板/税务参数并引导录期初；user_book 成员表（数据层多租户地基，
  admin 全可见、成员按授权可见，账套级访问校验 require_book_access）
- **账套成员管理**：用户管理页「账套授权」弹窗（勾选账套+账套内角色），4 个成员端点；
  **全部业务路由已挂账套访问校验**（凭证/报表/发票/附件/往来/税务/AI/结账，
  非成员跨账套访问一律 403）；删用户级联清理授权；最低可管性守护（账套不得无管理员）；
  全局角色=岗位（制审分离）× 账套成员=门禁（进哪些公司的账）
- 报表：科目余额表、资产负债表、利润表、现金流量表、总账/明细账、Excel 导出
- 辅助核算：四类往来单位（客户/供应商/员工/其他），科目挂辅助类型，凭证往来强校验

### 报表引擎
- **模板驱动取数**：报表行 = ReportTemplate 取数公式（科目+符号），支持在线编辑映射、一键恢复默认
- **表结法**：资产负债表"未分配利润" = 3103+3104+全部损益科目，期中报表恒满足
  资产 = 负债 + 所有者权益（对齐金蝶/用友口径）
- **映射体检页**：恒等式实时校验、未映射科目清单（有余额标红）、公式脏引用检测
- **现金流量行级标注**：分录现金行可指定 14 个流量项目（一张凭证拆多个流量项），
  未标注部分按"对方最大行科目"兜底推断，报表永不缺数
- **余额表未过账口径切换**：三档切换——仅已过账 / 含未过账（待审+已审）/ 含草稿；
  未过账金额走独立列（不计入已过账三栏、不破坏恒等式），含草稿档带预警提示
- **账务勾稽关系校验**（余额表 & 映射体检打通）：方向/余额异常（资产贷方、负债借方、货币资金赤字）、
  未分配利润↔净利润、货币资金↔现金流、跨期衔接四类校验，徽章式逐项 ✅/❌ + 异常明细展开

### AI 智能记账（主视图对话助手）
- 对话式记账：上传发票 PDF / 输入业务描述 → 解析 → 生成候选凭证卡片 → 人工确认落账
- 助手为标准主视图（与凭证/报表同交互模型），对话历史跨视图切换保持，首次进入有引导
- **支付截图直接记账**：微信/支付宝/银行支付记录自动识别（渠道/收付方向/对方/金额/时间/事项），
  AI 按用途生成凭证，零钱走 1012 其他货币资金、银行卡走 1002，分不清会主动问
- **多票据同图拆分**：一张照片拍两张发票、或发票+支付记录混拍，自动拆成多条候选逐张生成凭证
- **合同/报价单（docx）直接理解**：证据类文件提取全文（段落+表格）交 AI 理解生成凭证，
  卡片标注「建议附发票」提醒取得扣除凭据；超长自动截断，老版 .doc 引导另存
- 发票全量底册：金税 Excel 批量导入 + 单张 PDF 落账自动补录台账，销项/进项方向判定，
  发票↔凭证双向关联，发票行一键"AI记账"
- 凭证电子原件：落账自动挂附件（SHA256 校验），附件浮层管理（批量上传/删除，过账锁定）
- 价税规则按纳税人性质动态注入（小规模进项严禁拆税 / 一般纳税人双向分离）
- **AI 记账偏好（账套级）**：侧栏「🧩 AI 偏好」配置行业标签（软件/AI/电商/咨询/贸易/餐饮 6 包，可多选组合——AI 包含算力与大模型费用分摊、研发资本化确认、技术开发合同免税备案提醒）
  + 业务描述自由补充 + **自定义模板**（所见即所得编辑器，按标签自动生成后可直接修改，
  不选标签直接编写即纯自定义，手动修改后标签联动不覆盖、可一键重新生成）→ 注入智能体提示词
  理解业务背景（收入识别/科目倾向/摘要风格）；外包护栏文本永不凌驾核心记账规则，
  自定义模板为空回退标签合成，未配置零行为变化——多公司开放即用的模板化方案
- 往来单位"查找-确认-新建"流程，购方与账套公司不匹配时主动警告

### 系统与工程
- Docker 单容器部署，数据卷持久化，每日凌晨自动备份，Alembic 迁移启动自动执行
- 响应式适配（1280/800/640/400 四档，凭证行整体 zoom 缩放），移动端可用

## 五、设计亮点与创新

1. **三层护栏模式**（核心范式）：AI 生成账务数据必过三道关——
   ① Prompt 软约束（注入公司信息/日期/价税规则）→ ② 人工确认（卡片 + ask_user 追问）→
   ③ 代码硬兜底（重复拦截、往来 ID 净化、现金流量自动标注、价税规则强制）。
   模型不听话也污染不了账本。

2. **查重双保险（实质性重复导向）**：发票号精确命中已入账发票是金标准；金额通道收紧为
   **同日 + 同金额 + 科目类别一致 + 往来对象不冲突**（同一单据二次录入的特征）才提示——
   仅金额相同不拦（重复金额是正常业务），代码层 `_hard_check_duplicate` 同口径强制兜底。

3. **报表与 AI 解耦**：AI 只管把凭证做对，报表永远从已过账凭证经模板公式聚合——
   模型可换、公式可改，互不影响。

4. **表结法恒等式**：未分配利润公式吸收全部损益科目净额，任何时刻打开资产负债表都平衡，
   不依赖期末是否已结转。

5. **单文件零依赖前端**：约 2000 行原生 HTML/CSS/JS 承载全部交互，无构建链，
   在低配 NAS/家庭服务器上秒开。

## 六、使用指南（日常操作）

### 日常做账闭环
1. **录账**：AI 助手视图上传发票 PDF/图片、支付截图、合同 docx，或直接一句话描述业务 →
   AI 生成候选凭证（缺信息会追问；疑似重复、购方与账套不符会警告确认）
2. **确认落账**：核对凭证卡片（科目/金额/日期/往来/现金流量）→ 点「确认入账」生成草稿
3. **审核过账**：审核岗在凭证列表「审核通过 → 过账」（岗位分离，AI 无任何过账权限）
4. **看数**：科目余额表（三档未过账口径切换 + 顶部勾稽徽章）→ 报表模块三大表
5. **月末**：期末结转生成结转凭证 → 审核过账 → 结账（快照冻结，跨期衔接据此校验）

### 谁可以切换账套（心智模型）
**账号是登录凭证（决定能干什么活），账套成员是门禁卡（决定能进哪些公司的账）。**
admin 可切换所有账套（天然全权限）；制单/审核账号只能切换到被授权的账套，
未授权的账套连下拉选项都不会出现。授权入口：用户管理 → 账套授权。

### 角色与权限
- **admin**：全部权限 + 新建账套 + AI 偏好 / 模型设置 / 报表映射模板编辑，天然可见所有账套
- **bookkeeper（制单）**：录凭证、提交审核（账套访问按成员授权）
- **auditor（审核）**：审核、过账/反过账（与制单强制分离）

### AI 偏好（多公司模板化配置）
侧栏「🧩 AI 偏好」（仅管理员）：行业标签多选（软件/AI/电商/咨询/贸易/餐饮，可组合）
+ 业务描述 + 自定义模板编辑器（所见即所得）→ 影响新对话的科目选择与摘要风格；
护栏保证用户配置永不凌驾核心记账规则，未配置零行为变化。

### 勾稽体检在哪里看
科目余额表顶部徽章区 / 报表模块 → 映射体检页顶部：方向异常、未分配利润↔净利润、
货币资金↔现金流、跨期衔接四项，✅/❌ 逐项显示，异常点开明细直接定位科目。

## 七、下载、部署与升级（pre-test）

当前为 **pre-test 预发布测试版**，软件发布版本为 `v0.2.0-pretest.1`；上方 `v2.6` 是README内容修订号，两者不是同一种版本号。下载请到 [GitHub Releases](https://github.com/flyfox666/financebooking/releases)。

| 使用方式 | 前置条件 | 启动与地址 | 数据位置 |
|---|---|---|---|
| Windows安装版 | Windows 10/11 x64 | 运行 `YouShuLedgerAI-setup-*.exe` 安装，再从快捷方式启动 | `%LOCALAPPDATA%\有数LedgerAI\data` |
| Windows便携版 | Windows 10/11 x64 | 完整解压 `YouShuLedgerAI-portable-*.zip`，运行其中的 `YouShuLedgerAI.exe` | 同上，数据不在程序文件夹 |
| 本机隔离Docker测试 | Git、Docker Desktop/Engine及Compose插件 | 下方显式使用 `docker-compose.test.yml`；`127.0.0.1:18000/app` | 独立命名卷 `ledgerai-codex-test_test-data` |
| NAS/局域网Docker | 支持Docker的NAS/服务器 | 独立目录使用 `docker-compose.yml`；`服务器IP:8000/app` | 该部署目录的 `data/` |

### A. 下载Windows测试版

安装版和便携版均内置Python及依赖，无需安装Docker或Python。便携版必须连同 `_internal` 完整解压，不能只运行ZIP中的exe。首次启动会打开浏览器，默认本机8000端口被占用时在8000—8019中选择空闲端口，以控制台显示的地址为准。保留控制台窗口，关闭即停止服务。

首次空库自动创建 `admin / admin123456`。登录后在“用户管理”中修改密码，创建自己的账套，再到“模型设置”填写自己的API Key；下载包不含作者的Key或企业数据。Windows包仅绑定本机地址，未签名；下载后可用PowerShell的 `Get-FileHash 文件路径 -Algorithm SHA256` 与Release的 `SHA256SUMS.txt` 对照。

### B. 本机隔离Docker测试（本项目开发采用此方式）

```bash
git clone https://github.com/flyfox666/financebooking.git
cd financebooking
docker compose -f docker-compose.test.yml build backend
docker compose -f docker-compose.test.yml up -d backend
docker compose -f docker-compose.test.yml ps
```

访问 `http://127.0.0.1:18000/app`，空库使用默认管理员登录后修改密码并建账套。这份Compose内置仅供本地测试的配置，不读取根目录 `.env`，无需复制模型Key。停止服务用 `docker compose -f docker-compose.test.yml stop backend`。重建镜像保留数据卷；不要用 `down -v`，它会删除卷中的测试数据。

### C. NAS或局域网部署（另建目录，不在当前隔离测试目录运行）

安装Git、Docker及Compose插件，在独立目录克隆仓库。复制 `.env.example` 为 `.env`，把 `SECRET_KEY` 换成随机长字符串，LLM Key可留空，后续在网页配置。

```bash
docker compose -f docker-compose.yml build backend
docker compose -f docker-compose.yml up -d backend
docker compose -f docker-compose.yml ps
```

访问 `http://服务器内网IP:8000/app`。默认Compose的8000端口对宿主网络开放，只用于受控内网；本项目测试不执行这组命令。首次管理员登录和建账套同上，无须额外运行旧初始化脚本。完整步骤见 [NAS部署操作文档.md](NAS部署操作文档.md)。

### 升级、备份与Release约定

- 升级前下载并私有保存“完整备份”，退出Windows旧程序或停止对应Docker服务，再替换程序/更新源码并重建。启动会自动迁移数据库；回退时应恢复升级前备份，不能直接用旧程序打开迁移后的库。
- Windows卸载保留用户数据；便携版建议解压到新目录。Docker重建不会删除绑定目录或命名卷，但删除卷会丢数据。
- Release提供版本明确的安装版、便携ZIP、SHA256清单及构建依赖记录；GitHub自动生成的“Source code” ZIP只是源码，不是可双击运行的便携版。旧 `v0.1.0` 本地包缺少9月7日修复，不作为本次附件。
- 新版本使用新的 `vX.Y.Z-pretest.N` 标签，附变动说明、验收结果和已知限制，不覆盖旧版本附件。源文件在Git中，`build/`、`dist/` 的二进制产物仅作为经过验收的Release附件。
- 本地重建：以Python 3.12建立 `build/release-venv`，安装 `backend/requirements.txt` 和 `pyinstaller`，另备Inno Setup（含简体中文语言文件），运行 `installer/build-release.ps1 -Version 0.2.0-pretest.2`。可通过 `-Python`、`-Compiler` 指定工具路径。脚本生成包后仍须空库启动、登录/迁移、密钥检查，再创建预发布Release；它不会自动上传。
- **程序包不等于备份包**：系统“完整备份”含数据库、原件、待处理文件和 `.model_key` 等私密内容，不能上传GitHub或公开Release。密钥在运行时加密存库；`.env`、`.env.*`、数据目录与密钥文件被忽略，仅 `.env.example` 模板公开。忽略规则不能撤回历史提交，发布前仍需扫描历史与附件。

### 哪些文档已上传，哪些只在本机

已上传：README、CHANGELOG、开发进度与交接、测试覆盖计划、NAS部署操作文档、AGENTS、架构SVG、`docs/`设计说明、`installer/使用前必读.txt`与打包源码，以及历史 `.trae/rules/`。以仓库文件列表为准。

本机忽略：`dist/`旧安装包/便携包，`build/`工具链和验收日志，`.trae-html-share-packages/`设计网页分享ZIP，`social/`发布素材，根目录截图和票据PDF，依赖/缓存内的说明文档；这些不属于遗漏的项目使用说明。`.env`、数据库、原件与备份则是必须保留在本机的私有运行数据。

## 八、文档同步约定

- **本 README**：项目门面，阶段/功能/亮点变化时同步更新（版本号 + 日期）
- **开发进度与交接.md**：开发级细节（接口、迁移、实测记录、坑），每轮开发必读必写
- **测试覆盖计划.md**：CI 守护的业务规则清单，新功能登记用例（G1–G14）
- **项目架构图.svg**：架构变化时重绘（2026-09-04 已同步至当前形态）

---

*有数 LedgerAI · AI 记账，心里有数——一个和 AI 搭伙写出来的记账系统*
*（自用打磨中；多公司开放地基就绪：user_book 成员制 + 账套级权限校验 + AI 偏好模板化）*
