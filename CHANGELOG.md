# 变动记录

每次更新均记录具体变化、验证结果和交付范围，并同步《开发进度与交接.md》。通过必要验证后提交并推送 GitHub；敏感数据和本机构建产物不进入仓库。

## 2026-09-08 — pre-test分发与部署说明（v0.2.0-pretest.1）

- README区分Windows安装版/便携版、本机18000隔离Docker、另目录8000 NAS部署；说明首次密码、自己的模型Key、升级回退与数据位置。NAS文档修正首次建账和完整ZIP备份，移除只恢复数据库的旧指引。
- 更新安装包使用说明与版本参数，新增 `installer/build-release.ps1`，按新版本输出安装包/便携ZIP、SHA256和依赖记录。软件版本与README修订号分开，后续版本作为新的GitHub预发布Release，不覆盖旧附件。
- `.gitignore`增加 `.env.*`（仍保留 `.env.example`），继续忽略数据、密钥和构建产物。核对当前Key与所有已获取Git历史的444个文件对象，未命中；新包338个文件未含Key、数据库或解密文件。
- 从实际便携ZIP解压启动，独立空库迁移至 `f9a1b2c3d4e5`，管理员登录、创建合成账套、经营概览及企税待核对状态通过，模型配置表为空，SQLite完整性正常；验收进程已关闭。本轮无后端业务变更，不重跑昨日完整回归。
- 安装exe已编译，安装向导及真实旧版升级尚未端到端验收；本次明确为pre-test。9月6日旧v0.1.0包不作为本次附件。发布资产不包含账务“完整备份”ZIP。

## 2026-09-07 — 经营概览与税务核对（v2.6）

### 新增与优化
- 本期经营扩展为可展开的利润构成，支持本月/累计、毛利率和上月比较；资金区增加资金变化、经营现金净额及资金/往来分类。
- 新增“经营与资金”页面和分页分录追溯入口，可打开原凭证；手机隐藏右栏后仍可使用。
- 报表映射缺失显示待核对，提示未过账和计提完整性；沿用已过账及损益结转排除口径。
- 申报日历使用2026年官方日期，修正所属期和跨年提醒，支持按月/按季视图；未知年份显示待核验，移除未经认定的统一印花税/报表报送日。
- 企业所得税改用利润总额，并要求核对人数、资产、行业与预缴调整；缺资料、非公司、未核验年度不输出确定税额。网页与Excel同口径，导出附核对状态和日历来源。
- 导出携带登录认证；一般纳税人不套用小规模组合导出，利润映射异常返回可读错误。

### 验证与交付
- 完整回归282项通过、1项跳过；最终映射/导出补丁针对性回归28项通过，均有2项依赖弃用提示。
- 独立合成账套经不同制单/审核人完成6张真实附件凭证过账；验证月度/累计利润、现金变化、分录追溯及调整后企税预缴600元。
- 经营概览、税务页面通过1280/800/640/400宽度检查，主区域横向溢出为0；页面核算、按月日历及Excel导出完成交互验证。
- 重新登录后真实AI生成19元合成费用候选，人工确认仅产生第0006号草稿，凭证列表刷新可见；概览提示1张未过账，已过账利润/现金不变。候选四档宽度溢出为0、zoom为0.863/0.583/0.651/0.55。
- 已部署独立 `ledgerai-codex-test:18000`，无数据库结构变更，旧容器和财务数据未升级；Windows安装包未重建。
- 本轮交付第一阶段；报表准则版本化、票据池、AI业务追问和月结助手列为后续迭代，未宣称已实现完整申报。

## 2026-09-07 — 账务控制修复与 AI 优化

### 修复
- 批量凭证逐条校验所属账套；审核岗禁止制单和编辑；创建人及所有修改人不能审核对应凭证。
- 提交、审核、过账校验真实原件及 SHA256；待审或已审凭证增删原件后退回草稿重新审核。
- 拦截早于账套启用期间的凭证日期；发票按账套、方向、号码关联，购方为本公司时判为进项，保留红字金额。
- 限制同一原凭证重复红冲，修正反过账关联；修复 cf_item 输入丢失和净现金为零时遗漏外部现金流。
- AI 确认在事务内校验和写入，失败整体回滚；并发和同请求重试只生成一张凭证；多单据共享原件在全部处理前保留。
- 疑似重复和金额差异由用户核对风险指纹并填写理由，聊天文字不能作为授权；修复非法金额异常。

### 优化
- 候选卡片独立编辑；切换账套清理上下文并丢弃迟到回包；刷新后可恢复服务器保存的待处理候选，支持原件查看。
- AI 建议读取账套纳税人类型，硬拦截小规模进项拆税；对明确银行费用提供可解释的科目纠正；展示校验状态而非未经校准的置信度。
- AI 往来工具改为只读，缺失档案由用户手工创建；外部文字转义显示。
- 模型凭据加密；完整 ZIP 备份包含数据库、原件、待处理文件及解密文件，支持校验和空目录恢复，并展示备份失败状态。
- Windows 启动器按实例标识识别已有服务，遇到其他服务占用端口时选择空闲端口。
- 新增独立 Docker 测试编排；项目规范明确每轮更新变动记录、交接文档并提交推送。

### 验证与交付范围
- 完整回归：269 项通过、1 项因中文字体样本缺失跳过；最后补丁针对性回归：54 项通过。
- 真实 AI、多卡片编辑与确认、刷新恢复、跨账套迟到回包隔离、1280/800/640/400 宽度验证通过。
- 新测试库迁移至 `f9a1b2c3d4e5`，完整性检查通过；合成 PDF 原件逐字节匹配，完整备份实际恢复验证通过。
- 运行环境：独立容器 `ledgerai-codex-test`，地址 `http://127.0.0.1:18000/app`。原容器及财务数据未升级。
- 本次交付源码；Windows 安装包尚未重新构建。完整操作审计历史、真实匿名单据评测集和 OCR 字段更正仍待后续开发。
