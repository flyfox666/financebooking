# 变动记录

每次更新均记录具体变化、验证结果和交付范围，并同步《开发进度与交接.md》。通过必要验证后提交并推送 GitHub；敏感数据和本机构建产物不进入仓库。

## 2026-09-07 — 账务控制修复与 AI 优化

### 修复
- 批量凭证逐条校验所属账套；审核岗禁止制单和编辑；创建人及所有修改人不能审核对应凭证。
- 提交、审核、过账校验真实原件及 SHA256；待审或已审凭证增删原件后退回草稿重新审核。
- 拦截早于账套启用期间的凭证日期；发票按账套、方向、号码关联，购方为本公司时判为进项，保留红字金额。
- 限制同一原凭证重复红冲，修正反过账关联；修复 cf_item 输入丢失和净现金为零时遗漏外部现金流。
- AI 确认在事务内校验和写入，失败整体回滚；并发和同请求重试只生成一张凭证；多单据共享原件在全部处理前保留。
- 疑似重复和金额差异由用户核对风险指纹并填写理由，聊天文字不能作为授权；修复非法金额异常。

### 优化
- 候选卡片独立编辑；切换账套清理上下文并丢弃迟到回包；刷新后可恢复服务器保存的待处理候选，支持原件查看。
- AI 建议读取账套纳税人类型，硬拦截小规模进项拆税；对明确银行费用提供可解释的科目纠正；展示校验状态而非未经校准的置信度。
- AI 往来工具改为只读，缺失档案由用户手工创建；外部文字转义显示。
- 模型凭据加密；完整 ZIP 备份包含数据库、原件、待处理文件及解密文件，支持校验和空目录恢复，并展示备份失败状态。
- Windows 启动器按实例标识识别已有服务，遇到其他服务占用端口时选择空闲端口。
- 新增独立 Docker 测试编排；项目规范明确每轮更新变动记录、交接文档并提交推送。

### 验证与交付范围
- 完整回归：269 项通过、1 项因中文字体样本缺失跳过；最后补丁针对性回归：54 项通过。
- 真实 AI、多卡片编辑与确认、刷新恢复、跨账套迟到回包隔离、1280/800/640/400 宽度验证通过。
- 新测试库迁移至 `f9a1b2c3d4e5`，完整性检查通过；合成 PDF 原件逐字节匹配，完整备份实际恢复验证通过。
- 运行环境：独立容器 `ledgerai-codex-test`，地址 `http://127.0.0.1:18000/app`。原容器及财务数据未升级。
- 本次交付源码；Windows 安装包尚未重新构建。完整操作审计历史、真实匿名单据评测集和 OCR 字段更正仍待后续开发。
