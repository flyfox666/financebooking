# 有数 LedgerAI · 开发进度与交接文档

> 更新时间：2026-09-06
> 用途：任何新会话 / 新环境（Trae IDE 等）读此文档即可跟上进度。配合 `项目架构图.svg` 食用。

## 一、项目是什么

面向小规模纳税人的智能记账系统（自用：用户自己开的一人有限公司 OPC，非代账）：凭证、科目、报表、税务、发票 + AI 记账助手（主视图对话，上传发票/描述业务 → 生成候选凭证 → 人工确认落账）。

- **技术栈**：FastAPI + SQLAlchemy + SQLite（单文件库）+ 原生 HTML/CSS/JS 单页应用（无前端框架、无构建步骤）
- **部署**：两条并行交付线——①**Docker 单容器** `ledgerai-backend`（端口 8000，`docker compose up -d backend`，自用/多人协同）②**Windows 打包版**（PyInstaller + Inno Setup，免 Docker 免 Python，发给没有容器环境的用户，见「Windows 打包分发」章节）
- **代码位置**：`backend/app/` 下 api（13 个路由模块）/ ledger（业务：账务+税务+AI）/ models / schemas / core
- **前端**：`backend/app/static/app.html`（主界面，约 2000 行）+ `index.html`（登录/设置）
- **数据库**：Docker 版在容器内 `/data/ledger.db`；Windows 打包版在 `%LOCALAPPDATA%\有数LedgerAI\data\ledger.db`。均为每日凌晨 3 点自动备份

## 二、环境须知（重要）

1. 本机有**两套并行项目**：`d:\traecnproject\financebooking`（本项目）和 `d:\codexproject\financebooking`（用户另一独立项目，web:8088/api/db 三容器）。**不要碰 codexproject 那套**，容器名都带 `-1` 后缀，容易混淆。
2. 本地源码跑服务：全局 Python 3.13 缺依赖，已装到 `backend\.deps`，需 `PYTHONPATH=backend\.deps`（日常开发直接改代码 → `docker compose build backend && docker compose up -d backend` 即可）。
3. 登录账号：admin / admin123456。
4. LLM 服务在「设置」页配置（gateway 支持多 provider，密钥加密存储）。
5. **打包工具链**（均已就位，都在 gitignore 的 `build/` 下，不进仓库）：Python 3.12 打包 venv `build\venv`（含 PyInstaller + 应用依赖 + 测试用的 pytest/fpdf2/qrcode）；Inno Setup 7 便携版 `build\tools\inno\ISCC.exe`（`/PORTABLE=1` 装进项目目录，未写注册表、未碰系统目录）。重建方式见「Windows 打包分发」章节。
6. **跑测试必须 `cd backend` 再 `pytest tests`**（`PYTHONPATH=.`）。从项目根跑会读到根目录 `.env`——那是 Docker 用的，`ATTACHMENTS_DIR=/data/attachments` 在 Windows 下被解析成盘符根 `D:\data\attachments`（不可写）→ `test_ai_api.py` 的 `test_confirm_twice_rejected`、`test_discard_document` 两条报 `PermissionError`（这两条未使用 `attachments_dir` fixture，直连真实附件目录）。2026-09-05 实测对比：从根跑 239 passed / 2 failed，从 `backend` 跑 241 passed + 1 skipped（exit 0）——**这 2 条失败是 cwd 造成的假阳性，不是代码回归**。

## 三、已完成功能（截至 2026-09-01）

### 核心账务
- 凭证全生命周期：草稿 → 提交审核 → 审核通过/驳回 → 过账 → 反过账，角色权限（bookkeeper/auditor/admin）
- 科目体系（小企业会计准则 66 科目）、账套管理、期末结转
- 报表：余额表、利润表、资产负债表、现金流量表、总账/明细账、Excel 导出
- 税务模块：增值税/企税/个税/印花 + 申报日历

### 近期交互优化（已部署 Docker）
- **记账助手改主视图**（2026-09-02，方案A）：原为可拖拽/缩放/折叠的悬浮窗，现改为与凭证/报表同一交互模型的标准主视图；删除拖拽、八方向缩放、折叠、位置/尺寸 localStorage 持久化全套逻辑；首次进入显示欢迎引导气泡；chatLog 对话历史跨视图切换保持（switchView 重放）；agentChat/addTrace 对切走视图场景做了 null 防护；新增凭证/发票「AI记账」按钮统一改调 switchView('chat')
- **凭证常驻工具栏**：提交审核/审核通过/驳回/过账/反过账/删除草稿 固定在列表上方，未勾选时禁用
- **金额输入增强**：失焦自动两位小数；金额框按 `=` 自动填平衡金额（贷方=借差，借方反向可为负数提示）；修复空金额提交报 "Input should be a valid decimal"（normLines() 统一清洗）
- **发票购方校验**：agent.py 的 run_agent 注入账套公司名/税号到 system prompt，购方不匹配时 AI 须 ask_user 警告（已实测验证：天眼查发票触发警告）
- **报表/余额表内联月份选择器**（2026-09-03）：门楣“期间”控件在报表场景不显眼导致“无法选月份”的体验问题——报表模块与科目余额表标题旁各加一个明显的月份选择器，切换即按新月份重查当前视图/页签，并与门楣双向同步（reportPeriodChanged）；后端本就支持按 period 查询，纯前端 UX 修复

### 本期概要改造（2026-09-02 完成，已部署）
- 后端新增 `GET /api/reports/period-summary`（report_service.period_summary）：一次返回凭证状态计数 + 经营/财务 KPI，空值固定 "0.00"
- 前端 loadSummary 精简为三组：📄 凭证状态（可点击跳转筛选）· 📈 本期经营（收入/净利润）· 💰 资金与往来（货币资金/应收/应付）

### 现金流量行级标注（2026-09-02 完成，已部署）
- 数据层：voucher_line 新增 `cf_item` 列（迁移 b3e1c8f4d2a7），取值为现金流量表 14 个项目 key
- AI 路径：agent system prompt 规则 8——现金类科目（1001/1002/1012）行必须按经济实质填 cf_item
- 硬兜底：voucher_service._apply_cf_items——缺标/非法值自动按「对方最大行科目映射」填充，非现金行清除，报表永不缺数
- 报表：cashflow._flows 行级标注优先（一张凭证可拆多个流量项目），未标注部分退回旧推断
- 前端：凭证卡片/编辑器现金行出现「现金流量」下拉，详情表新增现金流量列；科目编码列靠左对齐
- 存量回填：backfill_cf.py 一次性脚本已对历史凭证现金行按同一兜底规则补齐（容器内执行，已跑过一次）

### 响应式适配（2026-09-02 完成，已部署）
- 根修：layout 中列 1fr→minmax(0,1fr)，nowrap 表格不再撑破网格；main 允许横向滚动
- 断点：≤1024px 隐藏右栏概要；≤680px 单列 + 侧边栏变顶部横滑导航 + topbar 换行
- 凭证行整体缩放：.vc-fit 包裹表头+分录行，fitVoucherLines() 按容器宽动态 zoom（下限 0.55），窄窗口借贷框始终完整可见；触发点=渲染/加行/window resize
- 验证方式：iframe 模拟 1280/800/640/400/520 宽，横向溢出全为 0

### 报表映射体检 + 模板编辑（2026-09-02 完成，已部署）
- 背景：报表取数公式其实存在 `ReportTemplate` 表（按账套隔离），formula 即「科目→报表行」映射，但之前无任何查看/维护入口
- 后端三个接口：
  - `GET /api/reports/template-check`（report_service.template_check）——行→科目映射全景（带符号+科目名）、未映射叶子科目清单（标红有余额的）、公式脏引用、资产负债恒等式校验
  - `PUT /api/reports/template-row`（仅 admin）——编辑指定 line 行的取数科目映射，校验科目存在/符号合法，calc 行拒绝编辑
  - `POST /api/reports/template-reset`（仅 admin）——删除该报表模板行并用默认模板重新播种（幂等）
- 前端：报表模块「映射体检」标签页：恒等式徽章 / 未映射科目表 / 两张报表行→科目映射全景 / 每行「编辑」按钮（内联科目选择器：±符号 + 66 科目下拉 + 添加/移除/保存/取消）/「恢复默认模板」
- **表结法改造**（对齐金蝶用友口径）：BS 默认模板「未分配利润」行 = 3103 + 3104 + 全部损益科目（14 个），期中报表恒平衡（之前损益未结转时资产≠负债+权益）；期末结转跑完后损益科目归零、3103 有值，公式两种状态都正确不重复计算。已用 template-reset 应用到账套 1，实测 11680.00 = 11680.00
- 实测：编辑→保存→体检实时刷新（改错时恒等式立刻 ❌、被移除科目立刻进未映射清单）→ 还原全通过

### AI 记账
- 智能体工具白名单：仅查询类 + ask_user，无过账/审核权限（会计责任硬边界）
- 发票 PDF 上传 → 结构化解析 → 生成候选凭证
- 往来单位（客户/供应商）查找-确认-新建流程
- **重复入账检测**（2026-09-02 完成；**2026-09-04 改口径为实质性重复**）：
  - 软约束：新工具 `check_duplicate`，提示词规则要求生成凭证前必须调用；新增 account_code/contact_id 参数供模型区分类别与往来
  - 硬约束：`_hard_check_duplicate` 代码层兜底——凭证返回前强制查重，命中即拦截转 ask_user；对话历史中出现过「疑似重复入账」警告且用户已回应则放行（避免死循环）
  - **2026-09-04 收紧（用户反馈：的确会有重复金额，仅金额相同不该拦）**：金额±30 天窗口废除 → 实质性重复 = 发票号精确命中已入账发票（金标准），或**同日 + 同金额 + 科目类别一致（含前缀匹配）+ 往来对象不冲突**（同一单据二次录入特征）；月度房租/同额不同供应商等正常业务不再误伤；用例 4→6
  - system prompt 注入当前日期（模型无法自行得知今天几号，曾因此编造 2025 年日期导致查重窗口偏移）
  - 已实测：320 元重复办公用品被拦截警告 → 用户确认后放行生成；35 元快递费无误伤
  - 注意（2026-09-02 修正）：发票号查重只匹配**已入账**（voucher_id 非空）的发票；台账已导入未入账的不算重复——否则"发票驱动记账"必然误伤

### 发票模块（2026-09-02 完成，已部署）
- **前端上传入口**：发票视图内置导入卡片（销项/进项 + 文件选择 + 结果反馈），不再需要去 /docs 调 API；方向筛选（全部/销项/进项）
- **发票↔凭证关联展示**：列表接口返回 voucher_id/voucher_no_display；AI 凭证落账时 `_link_invoice` 按发票号自动回写（PDF 结构化路径取 fields.invoice_no，文本路径从描述中正则提取，20 位号优先于 8 位）
- **发票驱动 AI 记账**：未关联凭证的正常发票行有「AI记账」按钮 → 切换到记账助手主视图并携带发票要素（发票号/日期/对方/品名/金额）发起对话 → 走完整 agent 流程（购方校验、往来确认、查重）→ 落账后列表自动刷新显示关联凭证号
- **往来 ID 净化硬兜底**：`_sanitize_contacts`——模型偶尔不调 find_or_create_contact 直接编造 contact_id，凭证返回前逐行校验，无效 ID 置空（卡片中手动补选）
- 已实测端到端：Excel 导入 2 张 → 一键 AI 记账 → 新建往来 → 查重放行 → 生成凭证 → 落账 → 发票行显示「记字第0007号」（测试数据已清理）

### 发票全量底册 + 电子原件（2026-09-02 下午完成，已部署）
- **PDF/图片落账自动补录台账**：`_create_invoice_from_fields`——AI 凭证落账时台账查不到该发票号，用解析字段自动补录一条 Invoice（方向按"购方税号/名称=账套公司 → 销项，否则进项"判断；开票日期保留票面原始日期，红冲取负数）。发票模块从此统管所有发票，金税 Excel 批量导入和单张 PDF 两条路径殊途同归；纯文本路径（source_kind=text）不补录（字段太少）
- **凭证电子原件查看**：AI 落账时 PDF/图片原件已自动存为凭证附件（`save_attachment` → `/data/attachments`，sha256 校验）；凭证详情页新增📎附件区——点击文件名在新窗口打开原件（blob URL），「＋上传原件」按钮支持手工凭证补传电子原件
- **价税规则按纳税人性质动态注入**（2026-09-02 完成，已部署）：system prompt 不再写死小规模口径，`_tax_rule` 按账套 `book.taxpayer_type` 注入——小规模：进项严禁拆税（价税合计全额进费用/资产，不动 2221）+ 销项按 1% 征收率价税分离；一般纳税人：双向价税分离，税率以票面为准。曾实测 AI 对 13% 进项专票错误套 1% 拆税，注入规则后同一场景全额进固定资产，已回归验证
- 已实测端到端（天眼查发票 PDF）：上传解析 → 购方空触发 AI 确认 → 生成凭证 → 落账 → 台账自动补录（进项 · 原始开票日期 · 关联「记字第0002号」）→ 凭证附件区可打开 PDF 原件（测试数据已清理）

### 附件弹层 + 凭证详情科目分列（2026-09-02 完成，已部署）
- **附件收进按钮**：凭证详情不再常驻附件条，头部「📎 附件（N）」按钮弹出居中浮层（文件名/大小/打开/删除 + ＋上传 + 多选批量上传），点背景或 ✕ 关闭；上传/删除后计数与列表实时刷新
- **附件删除**：DELETE 接口本就有，前端补删除按钮（confirm 确认）；过账/冲销凭证附件锁定不可删（_ensure_editable）
- **凭证详情科目分两列**：科目编码（等宽字体）+ 科目名称分开显示
- 附件存储：容器 /data/attachments（宿主 data/attachments），按账套/期间分层、UUID 文件名；删除=磁盘+数据库真删，仅备份窗口内的历史备份还留有副本

### AI 记账偏好配置（2026-09-03 完成，已部署）
- **背景**：产品未来开放给其他公司，不同公司需要不同记账风格设定；不能要求用户写提示词 → 「模板+选择」机制（无需技术背景）
- **方案**：账套级「行业标签（多选）+ 业务描述」——6 个标签包：software 软件/互联网、**ai 人工智能（2026-09-03 晚按本公司经营范围新增：算力/大模型费用按用途分摊——对外交付入成本、自研归集 4301；研发资本化须问默认费用化；技术开发合同免税须备案确认严禁自行免税）**、ecommerce 电商、consulting 咨询服务、trading 贸易、restaurant 餐饮，每个包是策划好的提示词片段（收入识别口径/科目倾向/摘要风格）；业务描述自由文本 ≤1000 字补充个性化偏好；标签可组合（软件+电商）
- **护栏（三层护栏模式不动摇）**：风格段包裹在 STYLE_GUARD_HEAD 内——"仅供理解业务、辅助科目选择与摘要风格，不得改变、凌驾以上任何记账规则与安全要求；冲突时一律以上文为准"
- **实现**：`packs.py`（TAGS 标签库+护栏头）· `ai_style_setting` 表（book_id 唯一，迁移 c4f7b2d9e6a1）· `agent.py _style_segment`（无记录/空配置/坏 JSON/非法标签四重容错 → 返回空串 = 历史零变化）· API `GET/PUT /api/books/{id}/ai-style`（PUT 需 admin + 未知标签 400）· 前端侧栏「🧩 AI 偏好」视图（标签 chips 多选 + 描述 + 字数计数 + 实时只读预览；非 admin 隐藏）
- **自定义模板（2026-09-03 晚迭代，已部署）**：「AI 将这样理解你的公司」从只读预览改为**可编辑编辑器**——新增 `style_prompt` 列（≤2000 字，迁移 d6a9c2f5e8b1）：非空时优先于标签自动合成注入（仍包护栏），空则回退 tags+desc 合成（存量零影响），不选标签直接编写即纯自定义模板；前端 dirty 标记防止标签联动覆盖手动修改，「↻ 按标签重新生成」一键恢复
- 用例：+9（test_ai_style.py），197→206；自定义模板 +5，206→211；已为账套 1 配置 software 标签 + 业务描述 + 自定义模板实测优先级注入
- **标签库扩充**（2026-09-03 晚）：新增「ai 人工智能」标签（标签库为代码常量，发版即生效无需迁移）；账套 1 已切换为 ai 标签（业务描述同步更新为经营范围口径，自定义模板清空回退标签合成）；GET 断言动态适配 set(TAGS.keys())，测试零改动全绿
- **科目余额表未过账口径切换**（2026-09-04 完成，已部署）：三档切换——仅已过账（默认，现状）/ 含未过账（待审+已审）/ 含草稿；未过账金额走**独立列**（不计入已过账三栏、不破坏平衡校验），含草稿档带预警提示。后端 `trial_balance(unposted=none/pending/all)` + `gross_sums` 抽 `statuses` 参数；前端 renderTB 加 select 切换、动态 7/9 列；用例 +3（213→216）
- **账务勾稽关系校验**（2026-09-04 完成，已部署）：新建 `ledger/check_service.py` + `/api/reports/checks`，四类校验——①方向/余额异常（按科目 direction 校验期末方向：资产贷方、负债/权益借方、收入借方、货币资金赤字 high）②未分配利润本年变动↔本年累计净利润（表结法，差异提示利润分配/盈余公积）③货币资金期末↔现金流量表期末④跨期衔接。前端余额表顶部 + 报表映射体检加「勾稽徽章」区（✅/❌ + 异常明细可展开）。用例 +6（216→222）
- **跨期衔接快照语义修正**（2026-09-04 晚，E2E 实测发现）：原实现「本期期初 vs 上期期末」两边均从 OpeningBalance+凭证现算，数学恒等——篡改期初两边同幅传导互相抵消，校验恒真抓不到错。修正为对比 **PeriodBalance 结账快照**（close_period 冻结）：抓结账后倒扎凭证/期初篡改/历史凭证改动；上期未结账明确标注跳过。实测：结账后篡改 1002 期初 777 元被抓（期初 464147.00 ≠ 快照期末 463370.00），恢复后 ✅。用例 +1（222→223）
- **账套管理与切换 P1**（2026-09-05 完成，已部署）：多租户地基一步到位——①`user_book` 成员表（迁移 e8f3a1c7d5b9，user×book×role 唯一约束；存量 admin 全部回填挂到既有账套）②`GET /api/books` 可见列表（admin 全部/成员按授权）+ 建账套自动挂创建者 admin 成员 + `require_book_access` 依赖③前端顶栏「有数 · 账套 ▾」下拉切换 + 「＋新建账套」表单（创建即预置科目/模板/税务参数并引导录期初）④切换隔离：chatLog 清空（防 A 账套上下文泄入 B 账套 AI 会话）、PERIOD 按账套 localStorage 记忆、主数据/体检缓存重建。用例 +10（223→233）
- **账套成员管理 + 全路由校验**（2026-09-05 完成，已部署，P2 提前落地）：①成员 4 端点（`GET/PUT /api/books/{id}/members`、`GET/PUT /api/users/{id}/books`，admin 专属）+ 最低可管性守护（清空后须有全局 admin 或账套 admin 成员，防授权断链）②删用户级联清理 user_book 授权（防孤儿）③**全部业务路由挂账套访问校验**：凭证 16 端点/报表 13/发票/往来/附件/AI/税务/结账——非成员跨账套访问一律 403（单实体端点先查实体归属账套再校验；body.book_id 端点函数体内校验）④前端用户管理页「账套授权」弹窗（勾选账套+账套内角色+解释文案）⑤心智模型：全局角色=岗位（制审分离），账套成员=门禁；admin 天然可见全部。测试适配：conftest book fixture 模拟真实授权流（建账套自动挂三测试用户），隔离用例用独立账套。用例 +9（233→242）
- **Windows 打包分发（2026-09-05 完成，产物已实测）**：让没有 Docker 环境的用户也能用——免装 Python、免装 Docker，双击即用。两种交付形态共享同一 PyInstaller 产物：
  - **① 绿色压缩包** `dist\有数LedgerAI-绿色版-v0.1.0.zip`（125.4 MB）：解压 → 双击 `YouShuLedgerAI.exe`。压缩包根含 `使用前必读.txt`
  - **② 安装程序** `dist\installer\YouShuLedgerAI-setup-v0.1.0.exe`（86.8 MB，Inno Setup 7 + lzma2 固态压缩）：中文向导、装到 `Program Files\YouShuLedgerAI`、桌面/开始菜单快捷方式、控制面板可卸载、安装完成页可勾选「立即启动」
  - **交付约定（用户拍板）**：`1A 空模板分发`——包内不含任何账套数据，用户首启自建库 + 自动创建 admin/admin123456；`2A 卸载保留数据`——卸载只删程序目录，账套数据留在用户目录，卸载完成后弹窗明确告知数据位置（防用户误以为账被套清空）
  - **数据与程序分离**：程序装 Program Files（只读），数据落 `%LOCALAPPDATA%\有数LedgerAI\data`（ledger.db / attachments / backups / .secret_key）→ 升级覆盖程序不丢账、发给别人不会带上自己的账
  - **实现要点**：
    - `backend/app/launcher.py`（PyInstaller 入口）：8000 端口已被占用则只开浏览器不重复起服务；否则起 uvicorn + 后台线程 2 秒后开浏览器
    - `core/config.py` 三分支数据目录：`LEDGER_DATA_DIR` 环境变量 > frozen 时 `%LOCALAPPDATA%\有数LedgerAI\data` > 开发环境 `./data`；`SECRET_KEY` **仅 frozen 环境**随机生成并持久化（防所有安装共用默认密钥导致 JWT 跨安装伪造），Docker/开发仍走 `.env`
    - `main.py`：`_bundle_root()`/`_static_dir()` 用 `sys._MEIPASS` 定位打包后的 alembic 迁移脚本与静态前端；新增 `seed_default_admin()` 挂进 lifespan（空模板首启建可登录账号，User 表非空则跳过）
    - 二维码识别改**双路径**：`parse.py` 优先 OpenCV `QRCodeDetector`（自包含），失败回退 pyzbar（Linux Docker 兜底）
    - `YouShuLedgerAI.spec`：onedir 模式（启动快、便于 Inno 装目录），datas 打包 alembic.ini + alembic/ + app/static，hiddenimports 补齐 uvicorn 运行时动态加载的 9 个实现模块
    - `installer/YouShuLedgerAI.iss`：AppId 固定 GUID（升级安装靠它识别旧版本，勿改）；`compiler:Languages\ChineseSimplified.isl` 内置前缀使脚本不依赖 Inno 安装位置；`CloseApplications=yes` 用 Restart Manager 提示关闭而非静默杀进程
  - **踩坑记录（重建时必看）**：
    1. **打包版误读项目根 `.env`** → `DATABASE_URL=sqlite:////data/ledger.db` 是 Docker 容器路径，Windows 下被解析成盘符根 `D:\data\ledger.db`。修复：frozen 环境 `env_file=None` + `get_settings()` 强制用本机数据目录覆盖（`_frozen_settings_kwargs()`）
    2. **pyzbar 的 `libzbar-64.dll` 依赖 VC++ 2013 运行库**（本机只有 2015-2022）→ 放弃 pyzbar 走 OpenCV，Docker 侧保留 pyzbar 兜底
    3. **cv2 5.0 的 `QRCodeEncoder` 生成的码 OpenCV 自己解不出**（测试工具坑，非产品缺陷）→ 冒烟测试改用 `qrcode` 库生成标准码
    4. **PyInstaller 构建被沙箱拦**：写系统 Python 目录的 `__pycache__` 被拒 → 构建前设 `$env:PYTHONDONTWRITEBYTECODE='1'`
    5. **`.iss` 必须存为 UTF-8 with BOM**：无 BOM 时 Inno 按 ANSI 解析，中文全乱码（`InfoBeforeFile` 指向的 txt 同理）
    6. **`{localappdata}` 在 admin 安装模式下指向安装者而非使用者** → 已删掉「打开数据文件夹」快捷方式（否则管理员装机、普通用户用时快捷方式指错目录），数据位置改由文档与卸载弹窗说明
  - **验证结果**：exe 启动 → Alembic 从空库迁移到 head → 自动建数据目录 → admin 登录返回有效 JWT → `/app` 200（92 KB）、`/settings` 200、`/api/health` ok → 二维码识别走 qr 层，发票号/开票日期/金额全对；绿色包 zip→解压→再跑通同一套验证
  - **Docker 零回归**：非 frozen 路径断言 `env_file=.env`、`DATABASE_URL/ATTACHMENTS_DIR/BACKUP_DIR/SECRET_KEY` 全部仍由 `.env` 与环境变量决定（环境变量优先级已单独断言）；`conftest.py` 的 client fixture 补 patch `seed_default_admin`（否则测试 lifespan 会去连真实库）
  - **已知限制**：①单机版，服务绑 127.0.0.1，局域网协同仍需用 Docker 版 ②未购买代码签名证书，首次运行会遇 SmartScreen「已保护你的电脑」，需点「更多信息 → 仍要运行」（已写进使用前必读）③仍是控制台窗口形态（关窗即停），托盘化留待后续
  - **空模板首启死胡同修复（2026-09-06 晨，实机验证发现）**：全新安装登录 admin 后报「当前账号没有可访问的账套，请联系管理员」并卡死在登录页——1A 空模板分发下数据库本来就没有账套，而原逻辑对空列表一律 alert + return，admin 也进不去（尽管 admin 有权建账套，`showNewBookForm/createNewBook` 都是现成的）。修复：`boot()` 空账套分支按角色分流——admin 进主界面直接渲染「📁 欢迎使用有数 LedgerAI · 创建你的第一个账套」引导（首启模式隐藏「取消」按钮防跳到无账套页面），非 admin 保持原提示。**顺带修复了 Docker 全新部署的同一死胡同**（前端改动两端共用）。E2E 验证（打包 venv 起空库服务 + 浏览器实测）：登录 → 引导表单 → 创建「冒烟测试科技有限公司」→ 自动跳期初余额录入，66 科目预置齐全。修复后产物已重新打包（zip 125.4MB / setup.exe 重编译零警告）
  - **重建命令**（改后端代码后重新出包）：
    ```powershell
    $env:PYTHONDONTWRITEBYTECODE='1'
    build\venv\Scripts\pyinstaller YouShuLedgerAI.spec --noconfirm          # → dist\YouShuLedgerAI\
    Compress-Archive -Path 'installer\使用前必读.txt','dist\YouShuLedgerAI' `
      -DestinationPath 'dist\有数LedgerAI-绿色版-v0.1.0.zip' -CompressionLevel Optimal -Force
    build\tools\inno\ISCC.exe installer\YouShuLedgerAI.iss                  # → dist\installer\
    ```

## 四、待办事项（优先级从高到低）

### 单据文件处理路线图（2026-09-02 定稿，分层策略）

**核心哲学**：文件分两种记账角色——
- **票据（凭据）**：发票、银行回单、行程单、支付截图——必须提取结构化字段→生成凭证
- **证据（附件）**：合同、报价单、验收单、Word 协议——不解析成凭证，挂凭证附件供日后查证

**优先级**：

1. **【BUG·✅ 已修复（2026-09-03 晨）】图片上传识别 HTTP 500**（2026-09-02 晚发现）
   - **现象**：对话助手上传 png/jpg 等图片 → `/api/ai/parse` 返回 500；已用合成 PNG 复现确认
   - **Root cause**：`parse.py` 的 `_to_data_url()` 调 `base64.b64encode`，但文件头 imports **漏了 `import base64`** → VLM 路径必抛 `NameError`
   - **影响面**：所有走视觉模型的路径——图片直传、无文字层 PDF、关键字段缺失的 PDF 补全
   - **修复内容**：① 补 `import base64`；② G9 冒烟用例锁真实链路（见测试覆盖计划 v1.2）；③ mock 粒度全量改造（只 mock gateway 层）
   - **E2E 验证**：同一张合成 PNG，修复前 500 → 修复后 200，真实 VLM 链路完整走通
2. **P0 解析异常全局兜底 ✅（2026-09-03）**：api/ai.py parse 端点三分支——VoucherError→400（类型不识别）；LLMError/未知异常→降级 200（`degraded:true` + 原件照存 staging + 前端提示"自动识别失败，原件已保存"），**解析失败是正常业务路径而非异常**
3. **P0 HEIC 支持 ✅（2026-09-03）**：requirements +`pillow-heif`；IMAGE_EXTS 加 .heic/.heif；opener 注册（未装依赖时优雅退化）；IMAGE_MIME 完整映射（顺带修掉 webp/bmp 误标 jpeg）
4. **P1 支付截图 + 多票据同图拆分 ✅（2026-09-03）**：
   - **支付截图**：VLM 提示词升级为「分类 + 数组输出」——每张票据输出 {"kind":"invoice"|"payment","fields":...}；payment 字段集 channel/pay_direction/counterparty/amount_total/pay_time/note；图中无发票候选时整图按支付单据处理（doc_type=payment）
   - **多票据拆分**：主结果取第一张发票（QR 层优先级不变），其余候选进 `extra_docs` → API 逐个建 AIDoc（共用 staging 原件）返回 `extra_doc_ids` → 前端逐张走 agent（pendingAsk 时中断并提示）
   - **agent 规则 10**：支付截图记账指引——零钱走 1012 其他货币资金/银行卡走 1002，分不清渠道或用途时 ask_user，voucher_date 取 pay_time 日期
   - **顺带修复**：金额归一化统一 quantize 两位小数（4 处：vlm/xml/qr/pdf_text）——模型输出 "320"（无小数位）此前会原样保留，现在统一 "320.00"
   - 用例：+4（test_ai_parse.py：payment 分类/两票拆分/混合图/API 多文档），189→193
5. **P1 docx 文本提取 ✅（2026-09-03）**：
   - **零新依赖**：docx 本质 zip+XML，extract_docx_text 用 zipfile+ElementTree 直读 word/document.xml（不引入 python-docx/libreoffice）；段落逐行、表格行用「 | 」连接单元格
   - **doc_type=reference（证据类）**：文本注入 fields.note（超 6000 字截断+提示），不当发票解析；老版 .doc（OLE）明确报错提示另存 docx；损坏 docx 友好报错
   - **agent 规则 11**：参考材料处理——理解业务要素生成凭证，reply 必须提醒「不能作为税前扣除凭据，请取得发票」，摘要注明依据（如「依据XX合同付首款」）
   - **前端徽章**：/api/ai/chat 响应带 doc_type → reference 凭证卡片显示「依据合同/报价单生成 · 建议附发票」，确认落账与重新生成按钮与 AI 卡片一致
   - 用例：+4（test_ai_parse.py：reference 提取/长文截断/.doc 拒绝/损坏 docx 拒绝），193→197
6. **P2 收件箱/票据池**（架构升级，配合前端）：所有文件先落库进统一票据池，AI 后台批量预解析打标（发票/回单/合同/未知），能出字段的出字段，出不来的标记待人工；前端一个列表扫一遍，能点的点 AI 记账，不能的挂附件；与现有发票台账自然融合
7. **P3 多页 PDF 每页独立过 VLM**：现在图片型 PDF 只取第一页转图，多票 PDF 会漏
8. 架构图 `项目架构图.svg` 随大改动同步更新（本期概要改造、重复检测、发票模块均未反映）
9. 可选优化：check_duplicate 关键字匹配目前不做硬过滤（金额+日期为主信号），后续可考虑摘要相似度
10. 可选优化：AI 生成凭证的 voucher_date 偶尔忽略用户给的开票日期而用今天（提示词已约束"未说明时用今天"，说明了开票日期时应优先采用，模型遵守不稳定；凭证卡片日期可手动改，落账前注意核对）
11. **【进行中·2026-09-06 更新】Windows 打包收尾 2 件事**：
    - ~~**① 重编译安装包**~~ ✅（2026-09-06 晨完成）：`使用前必读.txt` 已移入 `installer\`，重编译成功（46.6s，零 Warning），产物 `dist\installer\YouShuLedgerAI-setup-v0.1.0.exe` 86.8MB 已刷新。**新踩坑：双 BOM**——SearchReplace 编辑 `.iss` 会把已有 BOM 当普通字符保留再写一个新 BOM，文件头变成两个 `EF BB BF`，第二个 BOM 落在 line 1 行首使 `;` 注释失效 → ISCC 报 "Text is not inside a section"（line 1）。修复：`$t.TrimStart([char]65279)` 后以 UTF8Encoding($true) 重写。**以后每次用编辑工具改完 `.iss` 都要查双 BOM**。
    - **② 安装包实机安装验证**：2026-09-06 晨首轮验证发现「空模板首启死胡同」bug（详见已完成功能章节的修复记录），已修复并重打包。**待用户用新 setup.exe 复验**：覆盖安装（同 AppId 自动升级，数据保留）→ 登录 admin 应直接见「创建你的第一个账套」引导 → 建账套 → 期初录入 → 控制面板卸载后弹窗告知数据保留、且 `%LOCALAPPDATA%\有数LedgerAI\data` 确实还在
    - ~~**③ 提交推送**~~ ✅（2026-09-06 晨完成，共 3 个 commit 已推 origin/main）：①`5cb0647 feat(pack): Windows 打包分发——PyInstaller 绿色版 + Inno Setup 安装包`（10 文件 +481/-12，新增 spec/launcher/iss/使用前必读，改 .gitignore/config/main/parse/conftest/文档）②`35f859e docs: 打包收尾进度同步`③`e58bc2c fix(app): 空模板首启 admin 死胡同——无账套时引导创建第一个账套`（空模板首启修复）

## 五、关键文件索引

| 文件 | 说明 |
|---|---|
| `backend/app/static/app.html` | 全部前端 UI + 交互（视图切换/工具栏/金额逻辑都在这） |
| `backend/app/ledger/ai/agent.py` | AI 记账智能体（system prompt 含购方校验规则） |
| `backend/app/ledger/ai/parse.py` | 单据解析（PDF→结构化） |
| `backend/app/ledger/invoice_service.py` | 发票导入/列表 |
| `backend/app/api/` | 13 个路由模块（auth/vouchers/accounts/reports/tax/invoices/ai/...） |
| `docker-compose.yml` | 部署编排（backend 服务） |
| `backend/app/launcher.py` | Windows 打包版入口（起 uvicorn + 自动开浏览器 + 端口占用时只开浏览器） |
| `backend/app/core/config.py` | 数据目录三分支（`LEDGER_DATA_DIR` / frozen→LOCALAPPDATA / 开发→`./data`）+ SECRET_KEY 生成策略 |
| `YouShuLedgerAI.spec` | PyInstaller 打包配置（onedir，datas 含 alembic + 静态前端） |
| `installer/YouShuLedgerAI.iss` | Inno Setup 安装程序脚本（**必须 UTF-8 with BOM**，否则中文乱码） |
| `installer/使用前必读.txt` | 最终用户说明（启动方式/默认账号/数据目录/常见问题/卸载），源文件在 `installer\`，被打进两种交付形态 |

## 六、发布记录（社媒打卡）

- 2026-09-02：首帖（Day 4 阶段总结 + 功能截图）
  - 小红书：`social/ledgerai-day4/index.html`（标题「我家袁阿姨让我用AI写个记账软件」，5 图）
  - 公众号：`social/ledgerai-devlog/index.html`（标题「和AI搭伙写记账系统：第4天，阶段性汇报」，5 图，含悬浮窗实拍）
  - 项目背景设定：起因是「我家袁阿姨」（用户对母亲的爱称）用 Excel 记账 + 用户念叨 AI，跨界尝试（干中学干中测、顺便测模型），文风幽默诙谐；后续帖子沿用此人设，第一个内定用户=我家袁阿姨
  - 截图原图：`social/shots/`（凭证工具栏 / AI助手 / 手工凭证等号平衡 / 余额表 / 利润表；另有 2026-09-02 补充：demo-cf-*（AI 凭证+现金流量演示）、demo-responsive-*（窄屏适配）、demo-voucher-zoom（凭证行整体缩放））
  - 注意：截图用的是临时账号（已删除），顶栏显示名是演示用的「王会计」
