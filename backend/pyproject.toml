[project]
name = "ledgerai-backend"
version = "0.1.0"
description = "AI 财务记账与总账系统后端（阶段 A · M6）"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "sqlalchemy>=2.0.30",
    "pydantic>=2.7",
    "pydantic-settings>=2.3",
    "python-jose[cryptography]>=3.3",
    "bcrypt>=4.0",
    "httpx>=0.27",
    "openpyxl>=3.1",
    "python-multipart>=0.0.9",
    "alembic>=1.13",
    "pdfplumber>=0.11",
    "pyzbar>=0.1.9",
    "pillow>=10.0",
    "pymupdf>=1.24",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "fpdf2>=2.7",
    "qrcode>=7.4",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
