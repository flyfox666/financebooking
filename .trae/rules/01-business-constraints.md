alwaysApply: true
description: 有数 LedgerAI 项目业务硬约束。始终生效：所有对话（无论前端/后端/运维）在产生任何影响业务数据的决策前必须检查本规则不被违反。
---

# 业务硬约束（Always Apply）

## 不可绕过的红线（违反 = 数据错误 / 合规风险）

### 发票与台账
- 发票方向必须是 `sales`（开出发票/销项）或 `purchase`（收到发票/进项），不可省略或用其他值。
- 发票唯一性键 = `(book_id, direction, invoice_no)`，导入/补录均按此判重。
- **发票号查重仅匹配已入账发票**（`voucher_id NOT NULL`），台账已导入未入账不算重复（否则 AI 发票记账必然误拦）。
- 单张 PDF/图片落账**自动补录台账**；纯文本描述（source_kind=text）字段不足不补录。
- 小规模纳税人**进项严禁拆税**，价税合计全额入费用/资产；销项按 1% 征收率价税分离。一般纳税人双向分离，税率以票面为准。

### 凭证与权限
- **AI 无过账/审核权限**，候选凭证必须经用户确认才落账；工具白名单仅查询 + ask_user。
- 手工凭证 `source=manual` 必须至少附 1 张原始单据，否则 400 拦截。
- 过账/冲销凭证的附件**锁定不可删**，仅草稿/待审/已审三态可编辑。
- 删除附件 = 磁盘文件 `unlink()` + 数据库记录真删 + 重新 recount 凭证附件数。
- 凭证显示：科目编码与科目名称分两列；附件默认隐藏，头部按钮弹窗打开。

### 报表恒等式
- **表结法**：资产负债表「未分配利润」= 3103 + 3104 + 全部损益类科目净额（14 项），期中/期末恒等式必须成立。
- 现金流量：`voucher_line.cf_item` 行级标注优先（一张凭证可拆多个流量项），未标注走对方最大行科目兜底。

### 环境边界
- 只操作 `d:\traecnproject\financebooking`，**严禁碰** `d:\codexproject\financebooking`。
- 部署服务统一走 Docker Compose，不用全局 Python（缺依赖）。
- 浏览器自动化只用 Trae 内置 `agent-browser` Skill，**禁装** Playwright/Puppeteer/Selenium。
- 中国企业数据先 `search_companies` 锚定再查天眼查 MCP，禁止编造。

### 如果不确定
- 任何可能改变账务数据、余额、报表恒等式的操作，**先向用户确认再执行**。
- 报表取数规则、纳税人性质、科目编码三者联动修改时，改完必须跑一遍恒等式体检。
